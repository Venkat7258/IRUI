/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { StringDataPopupModelWithActionService } from './string-data-popup-model-with-action-service';

describe('Service: StringDataPopupModelWithActionService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [StringDataPopupModelWithActionService]
    });
  });

  it('should ...', inject([StringDataPopupModelWithActionService], (service: StringDataPopupModelWithActionService) => {
    expect(service).toBeTruthy();
  }));
});
