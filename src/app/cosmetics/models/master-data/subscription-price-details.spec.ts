import { SubscriptionPriceDetails } from './subscription-price-details';

describe('SubscriptionPriceDetails', () => {
  it('should create an instance', () => {
    expect(new SubscriptionPriceDetails()).toBeTruthy();
  });
});
